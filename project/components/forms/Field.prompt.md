**Field** — labelled form input for the contact panel. Uppercase tracked label over a hairline input on white; set `multiline` for the message box.

```jsx
<Field label="Company" value={v} onChange={set} />
<Field label="What do you want to achieve in Russia?" multiline value={m} onChange={setM} />
```
