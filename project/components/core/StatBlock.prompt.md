**StatBlock** — a large Archivo numeral over a terse label. Use in the proof band (light) and approach steps (`onDark`). Keep values as real, short fragments.

```jsx
<StatBlock value="90 days" label="to a working local entity, typically" />
<StatBlock value="01" label="Assess" onDark size="lg" />
```
