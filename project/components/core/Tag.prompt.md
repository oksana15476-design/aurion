**Tag** — pill/chip for sectors and markets. `outline` is the default rounded chip (colours gold on hover); `dashed` is the muted "and more" terminator; `solid` is the small ink square chip used for client-market labels.

```jsx
<Tag>Technology</Tag>
<Tag variant="dashed">and more</Tag>
<Tag variant="solid">China</Tag>
```
